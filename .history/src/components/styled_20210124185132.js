import {
  Window as WindowM,
  TitleBar as TitleBarM,
  Text as TextM,
} from "react-desktop/macOs"

import styled from "styled-components"

export const Window = styled(WindowM)``

export const TitleBar = styled(TitleBarM)``

export const Text = styled(TextM)``
